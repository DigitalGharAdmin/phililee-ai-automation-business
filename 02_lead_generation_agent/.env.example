# Export this variable in your shell; .env is not automatically loaded.
DATABASE_URL=sqlite:///./lead_generation.db
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# Optional AI key: leave blank for deterministic fallback. Never commit a real key.
# Local example only. Never commit credentials or real external database URLs.
